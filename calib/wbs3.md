## CCN3 tasks

```mermaid 
flowchart TD
   w1a(updating the objectives / describing 3 model scenarios?)
   w1b(study any match with ULTRASAT planner /coordinated observations/)
   w1c(Preparation of ULTRASAT workshop)

   w2(initial procurement & negotiations)
   w2b(refine definition of pipeline quality quotient)
   w2c(define Level1 till Level3 data products, declare *preliminary* header structure)
   w2d(Define requirements for pipeline design)
   w2e(Compare tools, define missing components)
   w2f(Calibration pipeline / preliminary tools)
   (Initial performance tests - can we cope with 4000 imgs/day?)
   (development structure of database - for E2E tests)
   (Mock Observation Plan)
   (define new operation mode/modes / adaptive exposure times based on AOCS stability?)
   (Transient detection - Xmatch of other observations)
   (Selection of GRB alerts for UV afterglow)
   (First connection to alert brokers / proposal of trigger selection mechanism)
   (selecting patches for worse localization)
   (coordinate plans with VZLU)
   (get PA plan / deliverable list from ASI/INAF)
   (development of a simulator of astronomical images for NUV payload)
   (study of astrophysical and instrumental effects on background / PSF)
```
